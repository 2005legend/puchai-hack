# Example .env variables
# Path to Firebase service account JSON (optional). If not provided, an in-memory fallback is used.
FIREBASE_CREDENTIALS_PATH=./serviceAccountKey.json
# PuchAI app key header (optional)
PUCHAI_APP_KEY=your_puchai_key_here
